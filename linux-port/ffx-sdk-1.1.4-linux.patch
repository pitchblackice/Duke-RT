diff --git a/ffx-api/CMakeLists.txt b/ffx-api/CMakeLists.txt
index 83e20ba..02c21b0 100644
--- a/ffx-api/CMakeLists.txt
+++ b/ffx-api/CMakeLists.txt
@@ -110,6 +110,17 @@ if (FFX_API_BACKEND STREQUAL DX12_X64 OR
 		${CMAKE_CURRENT_SOURCE_DIR}/include/ffx_api/dx12/*.hpp)
 endif()
 
+# The SDK's static components get linked into this shared library, so they must
+# be position-independent (not a concept on Windows).
+if(NOT WIN32)
+	set(CMAKE_POSITION_INDEPENDENT_CODE ON)
+endif()
+
+# ffx-api's own translation units need the same MSVC/CRT shims as the SDK.
+if(NOT WIN32)
+	add_compile_options("SHELL:-include ${CMAKE_CURRENT_SOURCE_DIR}/../ffx_linux_compat.h")
+endif()
+
 add_library(amd_fidelityfx_${FFX_PLATFORM_NAME} SHARED ${private_ffx_api} ${private_ffx_api_backend_dx12} ${private_ffx_api_backend_vk} ${public_ffx_api} ${public_ffx_api_backend_dx12} ${public_ffx_api_backend_vk})
 
 source_group("private_source" FILES ${private_ffx_api})
@@ -141,10 +152,33 @@ set(FFX_AUTO_COMPILE_SHADERS ON)
 set(FFX_BUILD_AS_DLL OFF)
 add_subdirectory(../sdk sdk)
 
-target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE ffx_fsr2_${CMAKE_GENERATOR_PLATFORM} ffx_fsr3upscaler_${CMAKE_GENERATOR_PLATFORM} ffx_opticalflow_${CMAKE_GENERATOR_PLATFORM} ffx_frameinterpolation_${CMAKE_GENERATOR_PLATFORM} DXGI)
+# The SDK suffixes its component libraries with its own platform name (x64),
+# which differs from ffx-api's FFX_PLATFORM_NAME (vk). CMAKE_GENERATOR_PLATFORM
+# supplied this on MSVC but is empty under Unix Makefiles.
+if(WIN32)
+	set(FFX_SDK_COMPONENT_SUFFIX ${CMAKE_GENERATOR_PLATFORM})
+elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64|arm64")
+	set(FFX_SDK_COMPONENT_SUFFIX arm64)
+else()
+	set(FFX_SDK_COMPONENT_SUFFIX x64)
+endif()
+
+target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE
+	ffx_fsr2_${FFX_SDK_COMPONENT_SUFFIX}
+	ffx_fsr3upscaler_${FFX_SDK_COMPONENT_SUFFIX}
+	ffx_opticalflow_${FFX_SDK_COMPONENT_SUFFIX}
+	ffx_frameinterpolation_${FFX_SDK_COMPONENT_SUFFIX})
+if(WIN32)
+	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE DXGI)
+else()
+	# The VK frame-interpolation swapchain runs its presenter/interpolation
+	# threads through the pthread-backed Win32 shims.
+	find_package(Threads REQUIRED)
+	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE Threads::Threads)
+endif()
 if (FFX_API_BACKEND STREQUAL VK_X64)
 	find_package(Vulkan REQUIRED)
-	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE Vulkan::Headers ffx_backend_vk_${CMAKE_GENERATOR_PLATFORM})
+	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE Vulkan::Headers ffx_backend_vk_${FFX_SDK_COMPONENT_SUFFIX})
 else() # DX12
-	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE D3D12 ffx_backend_dx12_${CMAKE_GENERATOR_PLATFORM})
+	target_link_libraries(amd_fidelityfx_${FFX_PLATFORM_NAME} PRIVATE D3D12 ffx_backend_dx12_${FFX_SDK_COMPONENT_SUFFIX})
 endif()
diff --git a/ffx-api/include/ffx_api/ffx_api.h b/ffx-api/include/ffx_api/ffx_api.h
index e136191..8825b61 100644
--- a/ffx-api/include/ffx_api/ffx_api.h
+++ b/ffx-api/include/ffx_api/ffx_api.h
@@ -26,7 +26,11 @@
 extern "C" {
 #endif  // #if defined(__cplusplus)
 
+#if defined(_MSC_VER)
 #define FFX_API_ENTRY __declspec(dllexport)
+#else
+#define FFX_API_ENTRY __attribute__((visibility("default")))
+#endif
 #include <stdint.h>
 
 enum FfxApiReturnCodes
diff --git a/ffx-api/src/backends.h b/ffx-api/src/backends.h
index 4ac2a54..7c8c1b3 100644
--- a/ffx-api/src/backends.h
+++ b/ffx-api/src/backends.h
@@ -23,7 +23,7 @@
 #pragma once
 #include "ffx_provider.h"
 #include <ffx_api/ffx_api.hpp>
-#include <FidelityFx/host/ffx_interface.h>
+#include <FidelityFX/host/ffx_interface.h>
 
 ffxReturnCode_t CreateBackend(const ffxCreateContextDescHeader* desc, bool& backendFound, FfxInterface* iface, size_t contexts, Allocator& alloc);
 
diff --git a/ffx-api/src/dx12/ffx_provider_framegenerationswapchain_dx12.cpp b/ffx-api/src/dx12/ffx_provider_framegenerationswapchain_dx12.cpp
index 9fac8ee..d2c55a7 100644
--- a/ffx-api/src/dx12/ffx_provider_framegenerationswapchain_dx12.cpp
+++ b/ffx-api/src/dx12/ffx_provider_framegenerationswapchain_dx12.cpp
@@ -42,7 +42,7 @@ struct InternalFgScContext
 uint64_t ffxProvider_FrameGenerationSwapChain_DX12::GetId() const
 {
     // FG SwapChain DX12, version 1.1.3
-    return 0xF65C'DD12'01'001'003ui64;
+    return 0xF65C'DD12'01'001'003ULL;
 }
 
 const char* ffxProvider_FrameGenerationSwapChain_DX12::GetVersionName() const
diff --git a/ffx-api/src/ffx_provider.cpp b/ffx-api/src/ffx_provider.cpp
index d7c0e64..bb314de 100644
--- a/ffx-api/src/ffx_provider.cpp
+++ b/ffx-api/src/ffx_provider.cpp
@@ -30,7 +30,9 @@
 #include <array>
 #include <optional>
 
+#ifdef FFX_BACKEND_DX12
 #include <d3d12.h>
+#endif
 
 #ifdef FFX_BACKEND_DX12
 #include "dx12/ffx_provider_framegenerationswapchain_dx12.h"
@@ -55,6 +57,9 @@ static constexpr size_t providerCount = _countof(providers);
 
 static std::array<std::optional<ffxProviderExternal>, 10> externalProviders = {};
 
+#ifdef FFX_BACKEND_DX12
+// The AMD driver-side provider override is exposed through a DX12/COM
+// interface and has no counterpart on other backends.
 MIDL_INTERFACE("b58d6601-7401-4234-8180-6febfc0e484c")
 IAmdExtFfxApi : public IUnknown
 {
@@ -131,10 +136,22 @@ void GetExternalProviders(ID3D12Device* device, uint64_t descType)
     
 }
 
+#else
+// Non-DX12 backends have no driver-side provider override; the external
+// provider table simply stays empty.
+static void GetExternalProviders(void* /*device*/, uint64_t /*descType*/)
+{
+}
+#endif  // FFX_BACKEND_DX12
+
 const ffxProvider* GetffxProvider(ffxStructType_t descType, uint64_t overrideId, void* device)
 {
     // check driver-side providers
+    #ifdef FFX_BACKEND_DX12
     GetExternalProviders(reinterpret_cast<ID3D12Device*>(device), descType);
+#else
+    GetExternalProviders(device, descType);
+#endif
 
     // If we are overriding, do not make the best provider choice decision
     if (overrideId)
@@ -217,7 +234,11 @@ uint64_t GetProviderVersions(ffxStructType_t descType, void* device, uint64_t ca
     uint64_t count = 0;
 
     // check driver-side providers
+    #ifdef FFX_BACKEND_DX12
     GetExternalProviders(reinterpret_cast<ID3D12Device*>(device), descType);
+#else
+    GetExternalProviders(device, descType);
+#endif
 
     for (const auto& provider : externalProviders)
     {
diff --git a/ffx-api/src/ffx_provider.h b/ffx-api/src/ffx_provider.h
index d501887..4e7d869 100644
--- a/ffx-api/src/ffx_provider.h
+++ b/ffx-api/src/ffx_provider.h
@@ -23,7 +23,7 @@
 #pragma once
 #include <ffx_api/ffx_api.hpp>
 #include <ffx_api/ffx_api_types.h>
-#include <FidelityFx/host/ffx_types.h>
+#include <FidelityFX/host/ffx_types.h>
 
 #define VERIFY(_cond, _retcode) \
     if (!(_cond)) return _retcode
diff --git a/ffx-api/src/ffx_provider_framegeneration.cpp b/ffx-api/src/ffx_provider_framegeneration.cpp
index 5b6e9d7..1b81003 100644
--- a/ffx-api/src/ffx_provider_framegeneration.cpp
+++ b/ffx-api/src/ffx_provider_framegeneration.cpp
@@ -77,7 +77,7 @@ struct InternalFgContext
 uint64_t ffxProvider_FrameGeneration::GetId() const
 {
     // FG, version from header
-    return 0xF600'0000ui64 << 32u | (FFX_SDK_MAKE_VERSION(FFX_FRAMEINTERPOLATION_VERSION_MAJOR, FFX_FRAMEINTERPOLATION_VERSION_MINOR, FFX_FRAMEINTERPOLATION_VERSION_PATCH) & 0xFFFF'FFFF);
+    return 0xF600'0000ULL << 32u | (FFX_SDK_MAKE_VERSION(FFX_FRAMEINTERPOLATION_VERSION_MAJOR, FFX_FRAMEINTERPOLATION_VERSION_MINOR, FFX_FRAMEINTERPOLATION_VERSION_PATCH) & 0xFFFF'FFFF);
 }
 
 const char* ffxProvider_FrameGeneration::GetVersionName() const
diff --git a/ffx-api/src/ffx_provider_fsr2.cpp b/ffx-api/src/ffx_provider_fsr2.cpp
index 1c9e551..ba0c2cb 100644
--- a/ffx-api/src/ffx_provider_fsr2.cpp
+++ b/ffx-api/src/ffx_provider_fsr2.cpp
@@ -79,7 +79,7 @@ bool ffxProvider_FSR2::CanProvide(uint64_t type) const
 uint64_t ffxProvider_FSR2::GetId() const
 {
     // FSR Scale, version from header
-    return 0xF5A5'CA1Eui64 << 32 | (FFX_SDK_MAKE_VERSION(FFX_FSR2_VERSION_MAJOR, FFX_FSR2_VERSION_MINOR, FFX_FSR2_VERSION_PATCH) & 0xFFFF'FFFF);
+    return 0xF5A5'CA1EULL << 32 | (FFX_SDK_MAKE_VERSION(FFX_FSR2_VERSION_MAJOR, FFX_FSR2_VERSION_MINOR, FFX_FSR2_VERSION_PATCH) & 0xFFFF'FFFF);
 }
 
 const char* ffxProvider_FSR2::GetVersionName() const
diff --git a/ffx-api/src/ffx_provider_fsr3upscale.cpp b/ffx-api/src/ffx_provider_fsr3upscale.cpp
index c1e140c..3e2908a 100644
--- a/ffx-api/src/ffx_provider_fsr3upscale.cpp
+++ b/ffx-api/src/ffx_provider_fsr3upscale.cpp
@@ -69,7 +69,7 @@ bool ffxProvider_FSR3Upscale::CanProvide(uint64_t type) const
 uint64_t ffxProvider_FSR3Upscale::GetId() const
 {
     // FSR Scale, version from header
-    return 0xF5A5'CA1Eui64 << 32 | (FFX_SDK_MAKE_VERSION(FFX_FSR3UPSCALER_VERSION_MAJOR, FFX_FSR3UPSCALER_VERSION_MINOR, FFX_FSR3UPSCALER_VERSION_PATCH) & 0xFFFF'FFFF);
+    return 0xF5A5'CA1EULL << 32 | (FFX_SDK_MAKE_VERSION(FFX_FSR3UPSCALER_VERSION_MAJOR, FFX_FSR3UPSCALER_VERSION_MINOR, FFX_FSR3UPSCALER_VERSION_PATCH) & 0xFFFF'FFFF);
 }
 
 const char* ffxProvider_FSR3Upscale::GetVersionName() const
diff --git a/ffx-api/src/vk/ffx_provider_framegenerationswapchain_vk.cpp b/ffx-api/src/vk/ffx_provider_framegenerationswapchain_vk.cpp
index 9239318..46ce089 100644
--- a/ffx-api/src/vk/ffx_provider_framegenerationswapchain_vk.cpp
+++ b/ffx-api/src/vk/ffx_provider_framegenerationswapchain_vk.cpp
@@ -92,7 +92,7 @@ bool ffxProvider_FrameGenerationSwapChain_VK::CanProvide(uint64_t type) const
 uint64_t ffxProvider_FrameGenerationSwapChain_VK::GetId() const
 {
     // FG SwapChain VK, version 1.1.3
-    return 0xF65D'564B'01'001'003ui64;
+    return 0xF65D'564B'01'001'003ULL;
 }
 
 const char* ffxProvider_FrameGenerationSwapChain_VK::GetVersionName() const
diff --git a/sdk/CMakeLists.txt b/sdk/CMakeLists.txt
index bfa1349..c54d81a 100644
--- a/sdk/CMakeLists.txt
+++ b/sdk/CMakeLists.txt
@@ -52,7 +52,11 @@ if (NOT FFX_CUSTOM_API)
 	endif()
 
 	# Pull in tool chain definitions
-	include(toolchain.cmake)
+	# toolchain.cmake force-sets CMAKE_GENERATOR_PLATFORM, which only the MSVC
+	# generators accept; Unix Makefiles rejects any platform specification.
+	if(WIN32)
+		include(toolchain.cmake)
+	endif()
 
 	# If we are NOT building the GDK, queue up DX12/VK solution for building
 	if (NOT FFX_GDK)
@@ -76,7 +80,14 @@ if (NOT FFX_CUSTOM_API)
 endif()
 
 # Set platform
-if(CMAKE_GENERATOR_PLATFORM STREQUAL "Gaming.Xbox.Scarlett.x64")
+if(NOT WIN32)
+	# Native Unix build: derive the platform name from the target processor.
+	if(CMAKE_SYSTEM_PROCESSOR MATCHES "aarch64|arm64")
+		set(FFX_PLATFORM_NAME arm64)
+	else()
+		set(FFX_PLATFORM_NAME x64)
+	endif()
+elseif(CMAKE_GENERATOR_PLATFORM STREQUAL "Gaming.Xbox.Scarlett.x64")
 	set(FFX_PLATFORM_NAME gdk_scarlett_x64)
 elseif(CMAKE_GENERATOR_PLATFORM STREQUAL "Gaming.Xbox.XboxOne.x64")
 	set(FFX_PLATFORM_NAME gdk_xboxone_x64)
@@ -158,6 +169,11 @@ if (FFX_SSSR)
 	option(FFX_DENOISER "Build FFX Denoiser API" ON)
 endif()
 
+# Non-Windows builds need shims for MSVC-only helpers (_countof, *_s, ...).
+if(NOT WIN32)
+	add_compile_options("SHELL:-include ${CMAKE_CURRENT_SOURCE_DIR}/../ffx_linux_compat.h")
+endif()
+
 # Add requred compile definitions
 add_compile_definitions(_UNICODE)
 add_compile_definitions(UNICODE)
@@ -169,7 +185,12 @@ if(FFX_VS_VERSION STREQUAL 2015 OR FFX_VS_VERSION STREQUAL 2017)
 endif()
 
 # Setup common variables
-set(FFX_SC_EXECUTABLE ${CMAKE_CURRENT_SOURCE_DIR}/tools/binary_store/FidelityFX_SC.exe)
+if(WIN32)
+    set(FFX_SC_EXECUTABLE ${CMAKE_CURRENT_SOURCE_DIR}/tools/binary_store/FidelityFX_SC.exe)
+else()
+    # Natively-built GLSL/SPIR-V shader compiler (see tools/ffx_shader_compiler).
+    set(FFX_SC_EXECUTABLE ${CMAKE_CURRENT_SOURCE_DIR}/tools/ffx_shader_compiler/bin/FidelityFX_SC)
+endif()
 set(FFX_INCLUDE_PATH ${CMAKE_CURRENT_SOURCE_DIR}/include)
 set(FFX_LIB_PATH ${CMAKE_CURRENT_SOURCE_DIR}/libs)
 set(FFX_BIN_PATH ${CMAKE_CURRENT_SOURCE_DIR}/bin/ffx_sdk)
diff --git a/sdk/include/FidelityFX/gpu/CMakeCompileShaders.txt b/sdk/include/FidelityFX/gpu/CMakeCompileShaders.txt
index ac1088d..947e040 100644
--- a/sdk/include/FidelityFX/gpu/CMakeCompileShaders.txt
+++ b/sdk/include/FidelityFX/gpu/CMakeCompileShaders.txt
@@ -127,7 +127,14 @@ function(compile_shaders_with_depfile
 		list(APPEND PERMUTATION_OUTPUTS ${WAVE64_16BIT_PERMUTATION_HEADER})
 	endforeach(PASS_SHADER)
 
-	set(${PERMUTATION_OUTPUTS} ${PERMUTATION_OUTPUTS} PARENT_SCOPE)
+	# PERMUTATION_OUTPUTS names the caller's output variable, but the
+	# list(APPEND PERMUTATION_OUTPUTS ...) calls above shadow the parameter, so
+	# that name ends up as element 0 of the list. Strip it before returning,
+	# otherwise the literal name becomes a build dependency and Make fails with
+	# "No rule to make target '<NAME>_PERMUTATION_OUTPUTS'".
+	set(_FFX_OUTPUT_VAR ${PERMUTATION_OUTPUTS})
+	list(REMOVE_AT PERMUTATION_OUTPUTS 0)
+	set(${_FFX_OUTPUT_VAR} ${PERMUTATION_OUTPUTS} PARENT_SCOPE)
 endfunction()
 
 # macro to add shader output files to a list for dependencies
diff --git a/sdk/include/FidelityFX/host/ffx_types.h b/sdk/include/FidelityFX/host/ffx_types.h
index ca9bc37..94e4d3c 100644
--- a/sdk/include/FidelityFX/host/ffx_types.h
+++ b/sdk/include/FidelityFX/host/ffx_types.h
@@ -77,7 +77,18 @@
 #define FFX_API __declspec(dllexport)
 #endif // #if defined (FFX_GCC)
 
+// The public contexts are opaque storage that must be at least as large as the
+// private ones. wchar_t is 4 bytes with GCC/Clang versus 2 with MSVC, which
+// inflates the private structs' name buffers past the Windows-tuned size (FSR2
+// measures 628600 bytes against a 524288 byte public context). Enlarging the
+// opaque storage off-MSVC costs only memory and keeps Windows byte-identical.
+#ifdef _MSC_VER
 #define FFX_SDK_DEFAULT_CONTEXT_SIZE (1024 * 128)
+#else
+// FSR3Upscaler is the largest at 837848 bytes, so 256K uint32 (1 MB) clears all
+// three contexts with headroom.
+#define FFX_SDK_DEFAULT_CONTEXT_SIZE (1024 * 256)
+#endif
 
 /// Maximum supported number of simultaneously bound SRVs.
 ///
diff --git a/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.cpp b/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.cpp
index 5c47b25..e3fc1ec 100644
--- a/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.cpp
+++ b/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.cpp
@@ -660,7 +660,7 @@ FfxErrorCode ffxConfigureFrameInterpolationSwapchainVK(FfxSwapchain gameSwapChai
         switch (key)
         {
             case FFX_FI_SWAPCHAIN_CONFIGURE_KEY_WAITCALLBACK:
-                pSwapChainVK->setWaitCallback(static_cast<FfxWaitCallbackFunc>(valuePtr));
+                pSwapChainVK->setWaitCallback(reinterpret_cast<FfxWaitCallbackFunc>(valuePtr));
             break;
             case FFX_FI_SWAPCHAIN_CONFIGURE_KEY_FRAMEPACINGTUNING:
                 if (valuePtr != nullptr)
@@ -1184,7 +1184,8 @@ DWORD WINAPI composeAndPresent_presenterThread(LPVOID pParam)
                     // if no frame was presented, we still need to update the semaphore
                     if (toWait.count > 0)
                     {
-                        presenter->presentQueue.submit(VK_NULL_HANDLE, toWait, SubmissionSemaphores());
+                        SubmissionSemaphores noSignals{};
+                        presenter->presentQueue.submit(VK_NULL_HANDLE, toWait, noSignals);
                     }
 
                     numFramesSentForPresentation = entry.numFramesSentForPresentationBase + entry.numFramesToPresent;
@@ -1357,8 +1358,11 @@ struct SwapchainCreationInfo
 
     VkImageCompressionControlEXT             imageCompressionControl;
     VkImageFormatListCreateInfo              imageFormatList;
+#ifdef VK_USE_PLATFORM_WIN32_KHR
+    // VK_EXT_full_screen_exclusive is a Win32-only extension.
     VkSurfaceFullScreenExclusiveInfoEXT      surfaceFullScreenExclusive;
     VkSurfaceFullScreenExclusiveWin32InfoEXT surfaceFullScreenExclusiveWin32;
+#endif
     VkSwapchainCounterCreateInfoEXT          swapchainCounter;
     VkSwapchainDisplayNativeHdrCreateInfoAMD swapchainDisplayNativeHdr;
     VkSwapchainPresentModesCreateInfoEXT     swapchainPresentModes;
@@ -1392,6 +1396,7 @@ VkResult getRealSwapchainCreateInfo(const VkSwapchainCreateInfoKHR* pCreateInfo,
         case VK_STRUCTURE_TYPE_IMAGE_FORMAT_LIST_CREATE_INFO:
             FFX_USE_PNEXT_AS_IS(imageFormatList, VkImageFormatListCreateInfo);
             break;
+#ifdef VK_USE_PLATFORM_WIN32_KHR
         case VK_STRUCTURE_TYPE_SURFACE_FULL_SCREEN_EXCLUSIVE_INFO_EXT:
             realSwapchainCreateInfo.surfaceFullScreenExclusive       = *reinterpret_cast<const VkSurfaceFullScreenExclusiveInfoEXT*>(pCurrent);
             realSwapchainCreateInfo.surfaceFullScreenExclusive.pNext = const_cast<void*>(realSwapchainCreateInfo.swapchain.pNext);  // because pNext is void* instead of const void* in vulkan header
@@ -1400,6 +1405,7 @@ VkResult getRealSwapchainCreateInfo(const VkSwapchainCreateInfoKHR* pCreateInfo,
         case VK_STRUCTURE_TYPE_SURFACE_FULL_SCREEN_EXCLUSIVE_WIN32_INFO_EXT:
             FFX_USE_PNEXT_AS_IS(surfaceFullScreenExclusiveWin32, VkSurfaceFullScreenExclusiveWin32InfoEXT);
             break;
+#endif
         case VK_STRUCTURE_TYPE_SWAPCHAIN_COUNTER_CREATE_INFO_EXT:
             FFX_USE_PNEXT_AS_IS(swapchainCounter, VkSwapchainCounterCreateInfoEXT);
             break;
@@ -2823,7 +2829,8 @@ VkResult FrameInterpolationSwapChainVK::submitCompositionOnGameQueue(const Pacin
         // if no frame was presented, we still need to update the semaphore
         if (toWait.count > 0)
         {
-            res = presentInfo.gameQueue.submit(VK_NULL_HANDLE, toWait, SubmissionSemaphores());
+            SubmissionSemaphores noSignals{};
+            res = presentInfo.gameQueue.submit(VK_NULL_HANDLE, toWait, noSignals);
         }
     }
 
diff --git a/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.h b/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.h
index 0218ade..17fd5cf 100644
--- a/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.h
+++ b/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK.h
@@ -41,7 +41,9 @@
 //   - On Windows, critical section and events are faster than their std counterparts
 //   - using Win32 threads to set the priorities
 //   - this needs to be ported to standard C++ or other platform if necessary
+#ifdef _WIN32
 #include <Windows.h>
+#endif
 
 #define FFX_FRAME_INTERPOLATION_SWAP_CHAIN_VERSION                     1
 #define FFX_FRAME_INTERPOLATION_SWAP_CHAIN_MAX_BUFFER_COUNT            6
diff --git a/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK_Helpers.h b/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK_Helpers.h
index 25f2c1e..ba7f2c0 100644
--- a/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK_Helpers.h
+++ b/sdk/src/backends/vk/FrameInterpolationSwapchain/FrameInterpolationSwapchainVK_Helpers.h
@@ -27,8 +27,12 @@
 #include <FidelityFX/host/ffx_assert.h>
 #include <FidelityFX/host/backends/vk/ffx_vk.h>
 
+#ifdef _WIN32
 #include <Windows.h>
+#endif
+#ifdef _WIN32
 #include <synchapi.h>
+#endif
 
 
 void waitForPerformanceCount(const int64_t targetCount);
diff --git a/sdk/src/backends/vk/ffx_vk.cpp b/sdk/src/backends/vk/ffx_vk.cpp
index d914479..4312fea 100644
--- a/sdk/src/backends/vk/ffx_vk.cpp
+++ b/sdk/src/backends/vk/ffx_vk.cpp
@@ -29,9 +29,8 @@
 
 #ifdef _WIN32
 #include <windows.h>
-#else
-#include <codecvt>  // this is deprecated so it's just a fallback solution
-#endif  // _WIN32
+#endif
+#include <math.h>   // floor, log2
 
 #include <vulkan/vulkan.h>
 
@@ -1117,17 +1116,64 @@ void ConvertUTF16ToUTF8(const wchar_t* inputName, char* outputBuffer, size_t out
     }
 }
 #else
+// std::wstring_convert is deprecated in C++17 and gone in newer libstdc++, so
+// transcode directly. wchar_t is UTF-32 here, making this a plain conversion.
 void ConvertUTF8ToUTF16(const char* inputName, wchar_t* outputBuffer, size_t outputLen)
 {
     memset(outputBuffer, 0, outputLen * sizeof(wchar_t));
-    std::wstring_convert<std::codecvt_utf8<wchar_t>, wchar_t> converter;
-    wcscpy_s(outputBuffer, outputLen, converter.from_bytes(inputName).c_str());
+    if (inputName == nullptr || outputLen == 0)
+        return;
+
+    size_t out = 0;
+    for (size_t i = 0; inputName[i] != '\0' && out + 1 < outputLen;)
+    {
+        const unsigned char c = (unsigned char)inputName[i];
+        uint32_t cp = 0;
+        size_t   extra = 0;
+        if (c < 0x80)                { cp = c;         extra = 0; }
+        else if ((c & 0xE0) == 0xC0) { cp = c & 0x1F;  extra = 1; }
+        else if ((c & 0xF0) == 0xE0) { cp = c & 0x0F;  extra = 2; }
+        else if ((c & 0xF8) == 0xF0) { cp = c & 0x07;  extra = 3; }
+        else { ++i; continue; }
+
+        bool truncated = false;
+        for (size_t k = 1; k <= extra; ++k)
+        {
+            if (inputName[i + k] == '\0') { truncated = true; break; }
+            cp = (cp << 6) | ((unsigned char)inputName[i + k] & 0x3F);
+        }
+        if (truncated)
+            break;
+
+        outputBuffer[out++] = (wchar_t)cp;
+        i += extra + 1;
+    }
+    outputBuffer[out] = L'\0';
 }
+
 void ConvertUTF16ToUTF8(const wchar_t* inputName, char* outputBuffer, size_t outputLen)
 {
     memset(outputBuffer, 0, outputLen * sizeof(char));
-    std::wstring_convert<std::codecvt_utf8<wchar_t>, wchar_t> converter;
-    strcpy_s(outputBuffer, outputLen, converter.to_bytes(inputName).c_str());
+    if (inputName == nullptr || outputLen == 0)
+        return;
+
+    size_t out = 0;
+    for (size_t i = 0; inputName[i] != L'\0'; ++i)
+    {
+        const uint32_t cp = (uint32_t)inputName[i];
+        char seq[4];
+        size_t len = 0;
+        if (cp < 0x80)          { seq[0] = (char)cp; len = 1; }
+        else if (cp < 0x800)    { seq[0] = (char)(0xC0 | (cp >> 6));  seq[1] = (char)(0x80 | (cp & 0x3F)); len = 2; }
+        else if (cp < 0x10000)  { seq[0] = (char)(0xE0 | (cp >> 12)); seq[1] = (char)(0x80 | ((cp >> 6) & 0x3F));  seq[2] = (char)(0x80 | (cp & 0x3F)); len = 3; }
+        else                    { seq[0] = (char)(0xF0 | (cp >> 18)); seq[1] = (char)(0x80 | ((cp >> 12) & 0x3F)); seq[2] = (char)(0x80 | ((cp >> 6) & 0x3F)); seq[3] = (char)(0x80 | (cp & 0x3F)); len = 4; }
+
+        if (out + len + 1 > outputLen)
+            break;
+        for (size_t k = 0; k < len; ++k)
+            outputBuffer[out++] = seq[k];
+    }
+    outputBuffer[out] = '\0';
 }
 #endif  // _WIN32
 
diff --git a/sdk/src/shared/ffx_message.cpp b/sdk/src/shared/ffx_message.cpp
index 7dd5127..911be61 100644
--- a/sdk/src/shared/ffx_message.cpp
+++ b/sdk/src/shared/ffx_message.cpp
@@ -21,6 +21,7 @@
 // THE SOFTWARE.
 
 #include <FidelityFX/host/ffx_message.h>
+#include <FidelityFX/host/ffx_util.h>   // FFX_UNUSED
 
 #ifdef _WIN32
 #ifndef WIN32_LEAN_AND_MEAN
diff --git a/sdk/tools/ffx_shader_compiler/CMakeLists.txt b/sdk/tools/ffx_shader_compiler/CMakeLists.txt
index 892685f..3006916 100644
--- a/sdk/tools/ffx_shader_compiler/CMakeLists.txt
+++ b/sdk/tools/ffx_shader_compiler/CMakeLists.txt
@@ -26,7 +26,9 @@ cmake_minimum_required(VERSION 3.17)
 project(FidelityFX_SC)
 
 # Enable multi-threaded compilation
-add_compile_options(/MP)
+if (MSVC)
+    add_compile_options(/MP)
+endif()
 
 # General language options (require language standards specified)
 set(CMAKE_CXX_STANDARD 17)
@@ -50,7 +52,7 @@ if (CMAKE_GENERATOR_PLATFORM STREQUAL x64 OR
 endif()
 
 # Check for Visual Studio 2019's build tooling
-if(MSVC_TOOLSET_VERSION VERSION_LESS 142)
+if(MSVC AND MSVC_TOOLSET_VERSION VERSION_LESS 142)
     message(FATAL_ERROR "Cannot find MSVC toolset version 142 or greater. Please make sure Visual Studio 2019 or newer installed")
 endif()
 
@@ -114,6 +116,15 @@ file(GLOB sources
 	"${CMAKE_CURRENT_SOURCE_DIR}/src/*.h"
 	"${CMAKE_CURRENT_SOURCE_DIR}/src/*.hpp")
 
+# The HLSL/DXBC path depends on DXC + Windows COM; exclude it elsewhere.
+if(NOT WIN32)
+	list(REMOVE_ITEM sources
+		"${CMAKE_CURRENT_SOURCE_DIR}/src/hlsl_compiler.cpp"
+		"${CMAKE_CURRENT_SOURCE_DIR}/src/hlsl_compiler.h"
+		"${CMAKE_CURRENT_SOURCE_DIR}/src/DXBCChecksum.cpp"
+		"${CMAKE_CURRENT_SOURCE_DIR}/src/DXBCChecksum.h")
+endif()
+
 # Append lib sources
 list(APPEND sources
 	${CMAKE_CURRENT_SOURCE_DIR}/libs/MD5/md5.h
@@ -126,13 +137,20 @@ add_executable(${PROJECT_NAME} ${sources})
 target_compile_features(${PROJECT_NAME} PRIVATE cxx_std_17)
 
 # Add external libs
-add_subdirectory(libs/agilitysdk)
-add_subdirectory(libs/dxc)
+if(WIN32)
+	add_subdirectory(libs/agilitysdk)
+	add_subdirectory(libs/dxc)
+endif()
 add_subdirectory(libs/glslangValidator)
 add_subdirectory(libs/tiny-process-library)
 
 # Add link time dependencies
-target_link_libraries (${PROJECT_NAME} dxguid agilitysdk dxc glslangValidator tiny-process-library)
+if(WIN32)
+	target_link_libraries (${PROJECT_NAME} dxguid agilitysdk dxc glslangValidator tiny-process-library)
+else()
+	# Vulkan/GLSL-only build: the DXC/Agility/dxguid stack is Windows-only.
+	target_link_libraries (${PROJECT_NAME} glslangValidator tiny-process-library)
+endif()
 target_include_directories (${PROJECT_NAME} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/libs/MD5
                                                    ${CMAKE_CURRENT_SOURCE_DIR}/libs/SPIRV-Reflect
                                                    ${CMAKE_CURRENT_SOURCE_DIR}/libs/tiny-process-library)
diff --git a/sdk/tools/ffx_shader_compiler/libs/glslangValidator/CMakeLists.txt b/sdk/tools/ffx_shader_compiler/libs/glslangValidator/CMakeLists.txt
index 9f9efa5..b7f581b 100644
--- a/sdk/tools/ffx_shader_compiler/libs/glslangValidator/CMakeLists.txt
+++ b/sdk/tools/ffx_shader_compiler/libs/glslangValidator/CMakeLists.txt
@@ -21,11 +21,13 @@
 # THE SOFTWARE.
 
 add_library(glslangValidator INTERFACE)
-target_include_directories(dxc INTERFACE BEFORE "inc/")
+target_include_directories(glslangValidator INTERFACE BEFORE "inc/")
 
 set(glslangValidator_binaries
     ${CMAKE_CURRENT_SOURCE_DIR}/bin/x64/glslangValidator.exe)
 
-copyTargetCommand("${glslangValidator_binaries}" ${CMAKE_HOME_DIRECTORY}/bin/Debug copied_glslangValidator_bin_debug)
-copyTargetCommand("${glslangValidator_binaries}" ${CMAKE_HOME_DIRECTORY}/bin/Release copied_glslangValidator_bin_release)
-add_dependencies(glslangValidator copied_glslangValidator_bin_debug copied_glslangValidator_bin_release)
+if(WIN32)
+	copyTargetCommand("${glslangValidator_binaries}" ${CMAKE_HOME_DIRECTORY}/bin/Debug copied_glslangValidator_bin_debug)
+	copyTargetCommand("${glslangValidator_binaries}" ${CMAKE_HOME_DIRECTORY}/bin/Release copied_glslangValidator_bin_release)
+	add_dependencies(glslangValidator copied_glslangValidator_bin_debug copied_glslangValidator_bin_release)
+endif()
diff --git a/sdk/tools/ffx_shader_compiler/src/ffx_sc.cpp b/sdk/tools/ffx_shader_compiler/src/ffx_sc.cpp
index c3ec37f..84efaed 100644
--- a/sdk/tools/ffx_shader_compiler/src/ffx_sc.cpp
+++ b/sdk/tools/ffx_shader_compiler/src/ffx_sc.cpp
@@ -20,21 +20,41 @@
 // OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER DEALINGS IN
 // THE SOFTWARE.
 
+#ifdef _WIN32
 #include "hlsl_compiler.h"
+#endif
 #include "glsl_compiler.h"
 #include "utils.h"
 
+#ifdef _WIN32
 #include <Windows.h>
 #include <pathcch.h>
+#endif
 #include <vector>
 #include <string_view>
 #include <filesystem>
 #include <unordered_set>
 #include <locale>
 #include <stdexcept>
+#include <cmath>
+#include <cstdio>
+#include <cerrno>
+
+#ifndef _WIN32
+// Minimal shim: the tool only ever opens files for writing with a wide path.
+static int _wfopen_s(FILE** outFile, const wchar_t* path, const wchar_t* mode)
+{
+    if (outFile == nullptr)
+        return EINVAL;
+    *outFile = std::fopen(WCharToUTF8(path).c_str(), WCharToUTF8(mode).c_str());
+    return *outFile != nullptr ? 0 : errno;
+}
+#endif
 
 
+#ifdef _WIN32
 #pragma comment(lib, "pathcch.lib")
+#endif
 
 static const wchar_t* const APP_NAME    = L"FidelityFX-SC";
 static const wchar_t* const EXE_NAME    = L"FidelityFX_SC";
@@ -309,6 +329,7 @@ void LaunchParameters::ParseCommandLine(int argCount, const wchar_t* const* args
 
 void LaunchParameters::EnsureOutputPathExistsAndMakeCanonical(std::wstring& inoutOutputPath)
 {
+#ifdef _WIN32
     std::replace(inoutOutputPath.begin(), inoutOutputPath.end(), L'/', L'\\');
 
     PWSTR canonicalOutputPath = NULL;
@@ -342,6 +363,26 @@ void LaunchParameters::EnsureOutputPathExistsAndMakeCanonical(std::wstring& inou
         inoutOutputPath = canonicalOutputPath;
         LocalFree(canonicalOutputPath);
     }
+#else
+    // POSIX: normalise separators, create the directory tree, and guarantee a
+    // trailing slash so callers can concatenate file names directly.
+    std::replace(inoutOutputPath.begin(), inoutOutputPath.end(), L'\\', L'/');
+    if (inoutOutputPath.empty())
+        return;
+
+    const fs::path outPath(inoutOutputPath);
+    std::error_code ec;
+    fs::create_directories(outPath, ec);
+
+    fs::path canonicalPath = fs::weakly_canonical(outPath, ec);
+    if (ec)
+        canonicalPath = outPath;
+
+    std::wstring result = canonicalPath.wstring();
+    if (!result.empty() && result.back() != L'/')
+        result.push_back(L'/');
+    inoutOutputPath = result;
+#endif
 }
 
 void LaunchParameters::ParsePermutationOption(PermutationOption& outPermutationOption, const std::wstring arg)
@@ -437,6 +478,7 @@ void Application::Process()
 
 std::wstring Application::MakeFullPath(const std::wstring & outputPath, const std::wstring & fileName)
 {
+#ifdef _WIN32
     // Append file name, optionally converting to long path again, because the outputPath alone could be normal path, but when filename is added -- it could become a long path
     PWSTR canonicalFileNameRaw = NULL;
     HRESULT hr = PathAllocCombine(outputPath.c_str(), fileName.c_str(), PATHCCH_ALLOW_LONG_PATHS, &canonicalFileNameRaw);
@@ -448,6 +490,12 @@ std::wstring Application::MakeFullPath(const std::wstring & outputPath, const st
         return canonicalFileName;
     }
     return fileName;
+#else
+    const fs::path combined = fs::path(outputPath) / fs::path(fileName);
+    std::error_code ec;
+    const fs::path canonicalPath = fs::weakly_canonical(combined, ec);
+    return ec ? combined.wstring() : canonicalPath.wstring();
+#endif
 }
 
 void Application::GenerateMacroPermutations(std::deque<Permutation>& permutations)
@@ -558,8 +606,12 @@ void Application::OpenSourceFile()
         std::wstring extension    = m_Params.inputFile.substr(extensionPos + 1, m_Params.inputFile.size() - extensionPos - 1);
 
         if (extension == L"hlsl")
+#ifdef _WIN32
             m_Compiler = std::unique_ptr<HLSLCompiler>(
                 new HLSLCompiler(HLSLCompiler::DXC, dxcDll, shaderPath, shaderName, shaderFileName, outputPath, m_Params.disableLogs, m_Params.debugCompile));
+#else
+            throw std::runtime_error("HLSL/DXC compilation is not available in this build (Vulkan/GLSL only). Use -compiler glslang with .glsl sources.");
+#endif
         else if (extension == L"glsl")
             m_Compiler = std::unique_ptr<GLSLCompiler>(new GLSLCompiler(glslangExe, shaderPath, shaderName, shaderFileName, outputPath, m_Params.disableLogs, m_Params.debugCompile));
         else
@@ -567,6 +619,7 @@ void Application::OpenSourceFile()
     }
     else
     {
+#ifdef _WIN32
         if (m_Params.compiler == L"dxc")
             m_Compiler = std::unique_ptr<HLSLCompiler>(
                 new HLSLCompiler(HLSLCompiler::DXC, dxcDll, shaderPath, shaderName, shaderFileName, outputPath, m_Params.disableLogs, m_Params.debugCompile));
@@ -580,6 +633,12 @@ void Application::OpenSourceFile()
             m_Compiler = std::unique_ptr<HLSLCompiler>(
                 new HLSLCompiler(HLSLCompiler::FXC, d3dDll, shaderPath, shaderName, shaderFileName, outputPath, m_Params.disableLogs, m_Params.debugCompile));
         else if (m_Params.compiler == L"glslang")
+#else
+        if (m_Params.compiler == L"dxc" || m_Params.compiler == L"fxc" ||
+            m_Params.compiler == L"gdk.scarlett.x64" || m_Params.compiler == L"gdk.xboxone.x64")
+            throw std::runtime_error("HLSL/DXC compilation is not available in this build (Vulkan/GLSL only). Use -compiler glslang.");
+        else if (m_Params.compiler == L"glslang")
+#endif
             m_Compiler = std::unique_ptr<GLSLCompiler>(new GLSLCompiler(glslangExe, shaderPath, shaderName, shaderFileName, outputPath, m_Params.disableLogs, m_Params.debugCompile));
         else
             throw std::runtime_error("Unknown compiler requested (valid options: dxc, fxc or glslang)");
@@ -1084,6 +1143,27 @@ void Application::DumpDepfileMSVC()
     printf("MSVC depfile not implemented yet.\n");
 }
 
+#ifndef _WIN32
+// POSIX entry point: transcode argv and hand off to the shared wmain body.
+int wmain(int argc, wchar_t** argv);
+
+int main(int argc, char** argv)
+{
+    std::vector<std::wstring> wideArgs;
+    wideArgs.reserve((size_t)argc);
+    for (int i = 0; i < argc; ++i)
+        wideArgs.push_back(UTF8ToWChar(argv[i]));
+
+    std::vector<wchar_t*> widePtrs;
+    widePtrs.reserve((size_t)argc + 1);
+    for (std::wstring& arg : wideArgs)
+        widePtrs.push_back(&arg[0]);
+    widePtrs.push_back(nullptr);
+
+    return wmain(argc, widePtrs.data());
+}
+#endif
+
 int wmain(int argc, wchar_t** argv)
 {
     try
diff --git a/sdk/tools/ffx_shader_compiler/src/glsl_compiler.cpp b/sdk/tools/ffx_shader_compiler/src/glsl_compiler.cpp
index 532b192..c7f9943 100644
--- a/sdk/tools/ffx_shader_compiler/src/glsl_compiler.cpp
+++ b/sdk/tools/ffx_shader_compiler/src/glsl_compiler.cpp
@@ -25,6 +25,9 @@
 
 #include <md5.h>
 #include <spirv_reflect.h>
+#ifndef _WIN32
+#include <unistd.h>
+#endif
 
 std::string MD5HashString(unsigned char* sig)
 {
@@ -36,7 +39,9 @@ std::string MD5HashString(unsigned char* sig)
 
     for (int i = 0; i < MD5_SIZE; i++)
     {
-        std::snprintf(out_ptr, 32, "%02x", sig[i]);
+        // Pass the space actually left in `out`; a fixed 32 overruns the
+        // 33-byte buffer once out_ptr advances (fatal under _FORTIFY_SOURCE).
+        std::snprintf(out_ptr, sizeof(out) - (size_t)(out_ptr - out), "%02x", sig[i]);
         out_ptr += 2;
     }
 
@@ -66,6 +71,34 @@ size_t GLSLShaderBinary::BufferSize()
     return spirv.size();
 }
 
+namespace
+{
+    // Resolve the glslang binary to use when the caller did not pass -glslangexe.
+    std::string DefaultGlslangExecutable()
+    {
+#ifdef _WIN32
+        return "glslangValidator.exe";
+#else
+        // Prefer a native binary staged alongside FidelityFX_SC so the tool is
+        // relocatable and does not depend on PATH.
+        char exePath[4096] = {};
+        const ssize_t len = readlink("/proc/self/exe", exePath, sizeof(exePath) - 1);
+        if (len > 0)
+        {
+            std::string dir(exePath, (size_t)len);
+            const size_t slash = dir.find_last_of('/');
+            if (slash != std::string::npos)
+            {
+                const std::string candidate = dir.substr(0, slash) + "/glslangValidator";
+                if (access(candidate.c_str(), X_OK) == 0)
+                    return candidate;
+            }
+        }
+        return "glslangValidator";
+#endif
+    }
+}
+
 GLSLCompiler::GLSLCompiler(const std::string& glslangExe,
                            const std::string& shaderPath,
                            const std::string& shaderName,
@@ -74,7 +107,7 @@ GLSLCompiler::GLSLCompiler(const std::string& glslangExe,
                            bool               disableLogs,
                            bool               debugCompile)
     : ICompiler(shaderPath, shaderName, shaderFileName, outputPath, disableLogs, debugCompile)
-    , m_GlslangExe(glslangExe.empty() ? "glslangValidator.exe" : glslangExe)
+    , m_GlslangExe(glslangExe.empty() ? DefaultGlslangExecutable() : glslangExe)
 {
     fs::create_directory(m_OutputPath + "/" + m_ShaderName + "_temp");
 }
diff --git a/sdk/tools/ffx_shader_compiler/src/pch.hpp b/sdk/tools/ffx_shader_compiler/src/pch.hpp
index c017698..1c70ac7 100644
--- a/sdk/tools/ffx_shader_compiler/src/pch.hpp
+++ b/sdk/tools/ffx_shader_compiler/src/pch.hpp
@@ -22,12 +22,14 @@
 
 #pragma once
 
+#ifdef _WIN32
 #define NOMINMAX
 #define WIN32_LEAN_AND_MEAN
 #include <Windows.h>
 #include <atlcomcli.h>
 #include <dxcapi.h>
 #include <d3dcompiler.h>
+#endif
 #include <assert.h>
 #include <stdio.h>
 #include <exception>
@@ -44,7 +46,9 @@
 #include <iostream>
 #include <algorithm>
 #include <fstream>
+#ifdef _WIN32
 #include <d3d12shader.h>
+#endif
 
 #include <filesystem>
 namespace fs = std::filesystem;
diff --git a/sdk/tools/ffx_shader_compiler/src/utils.cpp b/sdk/tools/ffx_shader_compiler/src/utils.cpp
index 0b92936..1ce49f5 100644
--- a/sdk/tools/ffx_shader_compiler/src/utils.cpp
+++ b/sdk/tools/ffx_shader_compiler/src/utils.cpp
@@ -22,6 +22,73 @@
 
 #include "utils.h"
 
+#ifndef _WIN32
+
+// On Linux wchar_t is 32-bit, so these are plain UTF-32 <-> UTF-8 transcodes.
+std::string WCharToUTF8(const std::wstring& wstr)
+{
+    std::string str;
+    str.reserve(wstr.size());
+    for (wchar_t wc : wstr)
+    {
+        const uint32_t cp = (uint32_t)wc;
+        if (cp < 0x80)
+        {
+            str.push_back((char)cp);
+        }
+        else if (cp < 0x800)
+        {
+            str.push_back((char)(0xC0 | (cp >> 6)));
+            str.push_back((char)(0x80 | (cp & 0x3F)));
+        }
+        else if (cp < 0x10000)
+        {
+            str.push_back((char)(0xE0 | (cp >> 12)));
+            str.push_back((char)(0x80 | ((cp >> 6) & 0x3F)));
+            str.push_back((char)(0x80 | (cp & 0x3F)));
+        }
+        else
+        {
+            str.push_back((char)(0xF0 | (cp >> 18)));
+            str.push_back((char)(0x80 | ((cp >> 12) & 0x3F)));
+            str.push_back((char)(0x80 | ((cp >> 6) & 0x3F)));
+            str.push_back((char)(0x80 | (cp & 0x3F)));
+        }
+    }
+    return str;
+}
+
+std::wstring UTF8ToWChar(const std::string& str)
+{
+    std::wstring wstr;
+    wstr.reserve(str.size());
+    for (size_t i = 0; i < str.size();)
+    {
+        const unsigned char c = (unsigned char)str[i];
+        uint32_t cp = 0;
+        size_t extra = 0;
+        if (c < 0x80)          { cp = c;         extra = 0; }
+        else if ((c & 0xE0) == 0xC0) { cp = c & 0x1F; extra = 1; }
+        else if ((c & 0xF0) == 0xE0) { cp = c & 0x0F; extra = 2; }
+        else if ((c & 0xF8) == 0xF0) { cp = c & 0x07; extra = 3; }
+        else { ++i; continue; }   // invalid lead byte - skip
+
+        if (i + extra >= str.size())
+        {
+            break;              // truncated trailing sequence
+        }
+        for (size_t k = 1; k <= extra; ++k)
+        {
+            cp = (cp << 6) | ((unsigned char)str[i + k] & 0x3F);
+        }
+        wstr.push_back((wchar_t)cp);
+        i += extra + 1;
+    }
+    return wstr;
+}
+
+#else
+
 std::string WCharToUTF8(const std::wstring& wstr)
 {
     if (wstr.empty())
@@ -49,3 +116,5 @@ std::wstring UTF8ToWChar(const std::string& str)
 
     return wstr;
 }
+
+#endif
diff --git a/sdk/tools/ffx_shader_compiler/src/utils.h b/sdk/tools/ffx_shader_compiler/src/utils.h
index ff57b45..88b735a 100644
--- a/sdk/tools/ffx_shader_compiler/src/utils.h
+++ b/sdk/tools/ffx_shader_compiler/src/utils.h
@@ -23,7 +23,9 @@
 #pragma once
 
 #include "pch.hpp"
+#ifdef _WIN32
 #include "DXBCChecksum.h"
+#endif
 
 std::string WCharToUTF8(const std::wstring& wstr);
 std::wstring UTF8ToWChar(const std::string& str);
